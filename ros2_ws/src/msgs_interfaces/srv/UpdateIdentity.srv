# UpdateIdentity.srv
# Service to update the identity of a tracked person (called by face recognition node)

# Request
int32 track_id
string identity
float32 confidence

---

# Response
bool success
string message
